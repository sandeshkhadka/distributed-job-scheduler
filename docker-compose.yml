version: "3.9"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: djs
      POSTGRES_USER: djs
      POSTGRES_PASSWORD: djs
    ports:
      - "5432:5432"
    volumes:
      - djs_data:/var/lib/postgresql/data

volumes:
  djs_data:
